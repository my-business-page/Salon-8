document.addEventListener('DOMContentLoaded',()=>{const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);const logo=$('.logo');const brand=(logo?.textContent||document.title.split('—')[0]||'SALON').replace(/\s+/g,' ').trim();let loader=$('#loader');if(!loader){loader=document.createElement('div');loader.id='loader';document.body.prepend(loader)}loader.innerHTML=`<div class="loader-mark"><strong>${brand}</strong><span>WELCOME</span></div>`;document.body.classList.add('is-loading');requestAnimationFrame(()=>setTimeout(()=>{loader.classList.add('is-hidden');document.body.classList.remove('is-loading');setTimeout(()=>loader.remove(),900)},850));const progress=document.createElement('div');progress.id='scroll-progress';document.body.append(progress);const glow=document.createElement('div');glow.id='cursor-glow';document.body.append(glow);const onScroll=()=>{const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h>0?scrollY/h*100:0)+'%';document.querySelector('header')?.classList.toggle('scrolled',scrollY>40)};addEventListener('scroll',onScroll,{passive:true});onScroll();if(matchMedia('(hover:hover)').matches){let tx=0,ty=0,x=0,y=0;addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY;glow.style.opacity='.8'});const tick=()=>{x+=(tx-x)*.12;y+=(ty-y)*.12;glow.style.left=x+'px';glow.style.top=y+'px';requestAnimationFrame(tick)};tick()}const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.1,rootMargin:'0px 0px -40px'});$$('.reveal').forEach(e=>io.observe(e));$$('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{if(matchMedia('(hover:none)').matches)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1000px) rotateX(${y*-5}deg) rotateY(${x*5}deg) translateY(-10px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});$$('.magnetic').forEach(btn=>{btn.addEventListener('pointermove',e=>{if(matchMedia('(hover:none)').matches)return;const r=btn.getBoundingClientRect();btn.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.1}px,${(e.clientY-r.top-r.height/2)*.1}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')});$$('a[href*="wa.me"]').forEach(a=>a.setAttribute('aria-label','Book via WhatsApp'));$$('a[href^="tel:"]').forEach(a=>a.setAttribute('aria-label','Call salon'));});